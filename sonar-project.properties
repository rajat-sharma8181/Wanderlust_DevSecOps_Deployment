sonar.projectKey=wanderlust
sonar.projectName=wanderlust

# Scan the whole repo; exclude vendored dependencies and build output so the
# analysis focuses on first-party source (node_modules is committed in this repo).
sonar.sources=.
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**
